AddEventListener{
para llamar a una funcion desde nuestro documento podemos hacerlo con onclick="funcion(event)" o podemos usar 
eventlistener de la siguiente forma

Ej:
document.querySelector("#idDeEtiqueta").addEventListener("tipoDeAccion (ej en un form es submit)", nombreDeLaFuncion);

variable.addEventListener("click",(evt*)=>{console.log(e)})

*cuando queremos referenciar al objeto mismo usamos e(evt,event,etc) dado que this no funciona en funciones flecha
}

operador spread{

si queremos igualar dos arrays, podemos usar el operador spread (...) de la siguiente forma:

arreglo1=[], arreglo2=[item,item]

arreglo1 = [...arreglo2]

asi nos queda arreglo1 = [item,item]
}

[git reset --hard para desechar los cambios que no fueron commiteados en el proyecto]

window.location.href = nos da la ruta de nuestro archivo

eventos:

-se puede agregar un evento cuando la pagina carga, para que se ejecute al cargar todo

	window.addEventListener("load",()=>{accion requerida})

-crear modal con js

	en html: 
		<button id="abrirModal">Abrir Modal</button>
		<div id="ventanaModal" class="modal">
   			 <div class="modal-content">
       			 <button id="cerrar">Cerrar</button>
       			 <h2>Ventana modal</h2>
    		</div>
 		 </div>
	
	en css:
		.modal {
  			display: none;
  			position: fixed;
			z-index: 1;
		}

	en js:
		var modal = document.getElementById("ventanaModal");
		var abrirmdl = document.getElementById("abrirModal");
		let btnCls = document.getElementById("cerrar");
